# Server Configuration
NODE_ENV=development
PORT=3001

# API Configuration
API_VERSION=v1
API_PREFIX=/api

# CORS Configuration (for development only - mobile apps don't use CORS in production)
# Comma-separated list of allowed origins (Expo web dev server, etc.)
CORS_ORIGIN=http://localhost:8081,exp://localhost:8081

# API Base URL (for mobile app to connect to backend)
# Development: Use your local IP (e.g., http://192.168.1.100:3001)
# Production: Your deployed backend URL (e.g., https://api.sawaapp.com)
API_BASE_URL=http://localhost:3001

# Database Configuration (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Local example: postgresql://postgres:password@localhost:5432/sawa_db
# Cloud example: postgresql://user:password@host.region.rds.amazonaws.com:5432/sawa_db
DATABASE_URL=postgresql://postgres:password@localhost:5432/sawa_db?schema=public

# Security
JWT_SECRET=your-secret-key-change-in-production
JWT_EXPIRES_IN=7d

# Redis (Optional - for caching and sessions)
# REDIS_URL=redis://localhost:6379

# External APIs (Add as needed)
# EXTERNAL_API_KEY=your-api-key
